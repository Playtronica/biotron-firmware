name: versioning

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  pre-release:
    name: "Pre Release"
    runs-on: ubuntu-latest

    steps:
      - name: Сheckout Repo
        uses: actions/checkout@main
        with:
          fetch-depth: 0

      - name: Get previous tag
        id: previoustag
        uses: "WyriHaximus/github-action-get-previous-tag@v1"

      - name: Calculate new tag
        id: newtag
        run: |
          echo "TAG_NAME=${{ steps.previoustag.outputs.tag }}" | awk -F. -v OFS=. '{$NF += 1 ; print}
        
      - name: "Build Firmaware"
        run: make build
        
      - name: "Rename Firmware"
        run: cp output/*.uf2 ${{ github.event.repository.name }}_${{ steps.previoustag.outputs.tag }}.uf2

      - name: "Build & test"
        run: |
          echo "done!"
          
      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: latest
          prerelease: true
          files: |
            *.uf2
      
      
  
